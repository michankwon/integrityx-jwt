# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# This file contains environment variables for the Next.js frontend.
# This file is automatically loaded by Next.js and should NOT be committed to version control.
# =============================================================================

# =============================================================================
# CLERK AUTHENTICATION CONFIGURATION
# =============================================================================
# These are the CLERK keys for authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_ZXZvbHZlZC1kcnVtLTE0LmNsZXJrLmFjY291bnRzLmRldiQ
CLERK_SECRET_KEY=sk_test_JgcXA9onj5ChuGjcl20HLB5HlQGaL1lpr479ZmEdTq

# =============================================================================
# PUBLIC VARIABLES (Exposed to Browser)
# =============================================================================
# These variables are accessible in the browser and should NOT contain secrets

# Walacor API Endpoint (Public - this is just the URL)
NEXT_PUBLIC_EC2_WALACOR=http://13.220.225.175

# AWS S3 Configuration (Public - bucket names and regions are not secret)
NEXT_PUBLIC_AWS_S3_BUCKET=integrityx-documents
NEXT_PUBLIC_AWS_REGION=us-east-1

# Application Configuration (Public)
NEXT_PUBLIC_APP_NAME=IntegrityX Financial Platform
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# =============================================================================
# SERVER-SIDE ONLY VARIABLES (Private - NOT exposed to browser)
# =============================================================================
# These variables are only available on the server and can contain secrets

# AWS Credentials (Server-side only - these are SECRET)
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# Database Configuration (Server-side only)
DATABASE_URL=sqlite:///./integrityx.db

# Session Configuration (Server-side only)
NEXTAUTH_SECRET=your-nextauth-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
NODE_ENV=development
