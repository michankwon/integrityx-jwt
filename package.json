{
  "name": "walacor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "lint:fix": "next lint --fix",
    "type-check": "tsc --noEmit",
    "push": "next lint --fix && next lint && npm run test:e2e && tsc --noEmit && next build && git add . && git commit -m 'walacor-commit' && git push && next dev"
  },
  "dependencies": {
    "next": "^14.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "@types/react": "^18.2.13",
    "@types/react-dom": "^18.2.7",
    "typescript": "^5.1.6",
    "eslint": "^8.44.0",
    "eslint-config-next": "^14.2.5"
  },
  "resolutions": {
    "rimraf": "^4.0.0",
    "glob": "^9.0.0"
  }
}