# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# This file contains example environment variables for the Next.js frontend.
# Copy this file to .env.local and fill in your actual values.
#
# IMPORTANT SECURITY NOTES:
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser (public)
# - Variables without NEXT_PUBLIC_ are server-side only (private)
# - NEVER put sensitive credentials in NEXT_PUBLIC_ variables
# - Walacor credentials should only be in backend/.env (server-side only)
# =============================================================================

# =============================================================================
# PUBLIC VARIABLES (Exposed to Browser)
# =============================================================================
# These variables are accessible in the browser and should NOT contain secrets

# Walacor API Endpoint (Public - this is just the URL)
NEXT_PUBLIC_EC2_WALACOR=http://your-walacor-instance.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_ZmlybS1veC04LmNsZXJrLmFjY291bnRzLmRldiQ 
# AWS S3 Configuration (Public - bucket names and regions are not secret)
NEXT_PUBLIC_AWS_S3_BUCKET=your-s3-bucket-name
NEXT_PUBLIC_AWS_REGION=us-east-1

# Application Configuration (Public)
NEXT_PUBLIC_APP_NAME=IntegrityX Financial Platform
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# =============================================================================
# SERVER-SIDE ONLY VARIABLES (Private - NOT exposed to browser)
# =============================================================================
# These variables are only available on the server and can contain secrets

# AWS Credentials (Server-side only - these are SECRET)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# Database Configuration (Server-side only)
DATABASE_URL=sqlite:///./integrityx.db

# Session Configuration (Server-side only)
NEXTAUTH_SECRET=your-nextauth-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# SECURITY WARNING
# =============================================================================
# ❌ NEVER DO THIS - These should be in backend/.env only:
# NEXT_PUBLIC_WALACOR_USERNAME=Admin
# NEXT_PUBLIC_WALACOR_PASSWORD=Th!51s1T@gMu
#
# ✅ CORRECT APPROACH:
# - Walacor credentials go in backend/.env (server-side only)
# - Frontend communicates with backend API
# - Backend handles Walacor authentication
# =============================================================================

# =============================================================================
# DEVELOPMENT vs PRODUCTION
# =============================================================================
# For development:
# - Use localhost URLs
# - Use test/development credentials
#
# For production:
# - Use production URLs
# - Use environment-specific credentials
# - Ensure all secrets are in server environment variables
# =============================================================================
